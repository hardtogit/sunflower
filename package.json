{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "eslint --cache --ignore-path .gitignore ./packages/*/src/**.js",
    "test": "jest",
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "pub": "lerna publish from-package"
  }
}
